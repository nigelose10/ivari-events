/**
 * boneyard registry stub — populated by `pnpm boneyard:build` after the
 * dev server captures live layouts. Until then, Skeleton components fall
 * back to a generic shimmer block so loading states still feel intentional.
 *
 * To populate: start the dev server (`pnpm dev`), open the routes you want
 * captured, then run `pnpm boneyard:build` in another terminal. This file
 * gets overwritten with real bones data.
 */
export const __BONEYARD_REGISTRY__ = {};
export default __BONEYARD_REGISTRY__;
